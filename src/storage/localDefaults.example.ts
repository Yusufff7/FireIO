// Template for localDefaults.ts — copy this file, rename to localDefaults.ts,
// and fill in your real values. localDefaults.ts is gitignored; this example
// file is the only one safe to commit.
//
// These become the app's out-of-the-box Settings so you don't have to type
// long URLs with a remote control. The Settings screen can still override
// them at runtime — this only sets what a fresh install starts with.
export const LOCAL_DEFAULTS = {
  torrentioBase: '',
  cometBase: '',
};
